#!/bin/bash
doiuse -b "ie >= 10, chrome >= 48, ff >= 45" css/style.css -i "flexbox, multicolumn, font-unicode-range, pointer-events, css-touch-action, pointer, css-appearance, css-filters, text-size-adjust, transforms3d, text-size-adjust"
